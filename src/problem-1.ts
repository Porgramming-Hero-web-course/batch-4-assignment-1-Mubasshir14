{
    // 

    function sumArray(number: number[]): number {
        const totalSum = number.reduce((a, n) => a + n, 0);
        return totalSum;
    }


    // const result = sumArray([1, 2, 3, 4, 5]);;
    // console.log(result);


    // 
}